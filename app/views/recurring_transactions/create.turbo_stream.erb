<%= turbo_stream.append "recurring_transactions_list" do %>
  <%= render partial: "recurring_transactions/recurring_transaction", locals: { recurring_transaction: @recurring_transaction } %>
<% end %>

<%= turbo_stream.replace "modal" do %>
  <turbo-frame id="modal"></turbo-frame>
<% end %>
